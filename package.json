{
  "name": "js86emu",
  "version": "0.3.0",
  "description": "x86 emulator written in Javascript.",
  "author": "Chad Rempp",
  "license": "GPL-2.0",
  "repository": "https://github.com/crempp/js86emu",
  "main": "compiled.js",
  "dependencies": {
    "winston": "^2.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-preset-env": "^1.1.8",
    "jest": "^19.0.2"
  },
  "scripts": {
    "build": "babel src -d dist --source-maps --presets env",
    "build:watch": "npm run build -- -w",
    "test": "jest",
    "runner": "node ./dist/runner.js"
  }
}
