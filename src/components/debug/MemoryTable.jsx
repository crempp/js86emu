import React, {Component} from "react";
import { styled } from "../../stitches.config";

export default class MemoryTable extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>Memory Table</div>
    );
  }
}